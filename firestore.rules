rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // App uses Firebase Admin SDK on the server only; block direct client access.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
